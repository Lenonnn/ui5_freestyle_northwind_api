sap.ui.define([
    "./BaseController"
],
function (BaseController) {
    "use strict";

    return BaseController.extend("test.env.lenon.appproductsnorthwind.controller.App", {
        
        onInit: function () {
        }

    });
});
